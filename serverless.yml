service:
  name: serverless-experiments

plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs10.x
  stage: production
  region: eu-west-1

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: hello
